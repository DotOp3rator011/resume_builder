import React from "react";


const Skills = (props) => {

    const {} = props;

    return (
        <div className={"skills-container"}>
            <span className={"skill-item"}>Python / Django</span>
            <span className={"skill-item"}>Node JS</span>
            <span className={"skill-item"}>Microservices</span>
            <span className={"skill-item"}>RESTful Web Services</span>
            <span className={"skill-item"}>React JS</span>
            <span className={"skill-item"}>MySQL</span>
            <span className={"skill-item"}>Mongo DB</span>
        </div>
    )
};

export default Skills
